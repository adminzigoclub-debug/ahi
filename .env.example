# Copiar este archivo a ".env" y completar con valores reales.
# ".env" nunca se sube al repositorio (ver .gitignore).

# --- Supabase (preparado, aún no implementado) ---
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# --- Metadatos del sitio (usados en SEO / Open Graph) ---
VITE_SITE_URL=https://ahi.com
VITE_SITE_NAME=ahí
